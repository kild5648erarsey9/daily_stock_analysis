# Core dependencies for daily stock analysis
akshare>=1.10.0
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
seaborn>=0.12.0
requests>=2.28.0
python-dotenv>=0.21.0
schedule>=1.1.0
loguru>=0.6.0

# Data storage
# sqlite3  # built-in, no install needed

# Email notifications
# smtplib  # built-in, no install needed

# Optional: enhanced charting
mplfinance>=0.12.9

# Optional: technical analysis indicators
ta>=0.10.2

# Development dependencies
pytest>=7.2.0
pytest-cov>=4.0.0
black>=22.12.0
flake8>=6.0.0

# Personal additions
ipython>=8.0.0  # useful for interactive exploration in Jupyter
jupyter>=1.0.0  # for running analysis notebooks locally
notebook>=6.5.0  # pinning notebook separately for stability with jupyter
plotly>=5.11.0  # interactive charts -- much nicer for personal exploration
rich>=13.0.0  # pretty terminal output, helpful when running scripts manually
openpyxl>=3.1.0  # export analysis results to Excel for easy sharing
kaleido>=0.2.1  # needed for exporting plotly charts to static images (PNG/PDF)
tabulate>=0.9.0  # nicely formatted tables when printing DataFrames to terminal
